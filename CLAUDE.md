# Claude instructions

This workspace's full agent brief is in `AGENTS.md` at the root — read it first.

Short version: in `ideas/` your job is to attack ideas, not encourage them. Under
`projects/` read that project's own `AGENTS.md` and work one build-plan task at a time.
Never put real customer data anywhere in this repo.
